exports.companyTestSwagger = (req, res, next) => {
  // #swagger.summary = 'test router for the the test path'
  // #swagger.description = 'this route is for testing the auth tag routes have no use for the app just for testing.'
  next()
}
