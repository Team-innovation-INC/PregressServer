exports.companyAuthTag = (req, res, next) => {
  // #swagger.tags = ['-- AUTHENTICATION COMPANY --']
  // #swagger.summary = 'summary for the company route...'
  // #swagger.description = 'description for the company route...'
  next()
}

exports.companyProviderTag = (req, res, next) => {
  // #swagger.tags = ['-- PROVIDER COMPANY --']
  // #swagger.summary = 'summary for the company providers integration route...'
  // #swagger.description = 'this is used for the providers managements tasks for the providers uses at the app'
  next()
}